{% extends "WikiIndexBundle::layout.html.twig" %}

{% block body %}
{% include "::alert.html.twig" %}
<div class="row">
	<div class="col-lg-12">
		<div class="actionbar">
			<ul>
				<li><a href="{{ path('wiki_articles_update' , { id : article.id }) }}" class="btn btn-success"><span class="glyphicon glyphicon-pencil"></span> Éditer</a></li>
				<li><a href="{{ path('wiki_articles_delete' , { id : article.id }) }}" class="btn btn-danger"><span class="glyphicon glyphicon-trash"></span> Supprimer</a></li>
			</ul>
		</div>
	</div>
</div>
<div class="markdown">
	{{ ('## ' ~ article.title)|markdown }}
	{{ article.content|markdown }}
</div>
{% endblock %}